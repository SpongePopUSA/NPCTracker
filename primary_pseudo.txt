import openpyxl

importData ():
    # Open workbook 
    # Open spreadsheet
    # Iterate across NPC table
        # Store each in a list of NPC's
    # Iterate across event table
        # Store each in a list of events

    # Return current state
        # Current state includes workbook, worksheet, NPC list, Event list

main ():
    # importData()
    # Import data from spreadsheet

    # getInput()
    # Modify data based on user input

    # saveAndQuit()
    # Make backup of spreadsheet for debugging
    # Save current state to spreadsheet

main()